# Optional — not required for the default Linode setup.
# Default: C++ serves UI + API on :8080 (see docs/linode-deploy.md).
#
# Only use this if you later want HTTPS on a real domain.

# api.example.com {
# 	handle /api/* {
# 		reverse_proxy 127.0.0.1:8080
# 	}
# 	root * /var/www/transfera
# 	file_server
# }
